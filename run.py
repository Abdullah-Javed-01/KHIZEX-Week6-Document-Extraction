from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parent))
print("The submission package already includes a completed evaluation run in results/.\nUse notebooks/01_document_extraction_walkthrough.ipynb for a walkthrough, or import src.pipeline for new documents.")
